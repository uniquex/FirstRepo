/*
package com.example.liquibase.Model.Mapper;

import com.example.liquibase.Entity.ClubEntity;
import com.example.liquibase.Model.Dto.ClubDto;
import com.example.liquibase.Model.Request.ClubRequest;
import org.mapstruct.Mapper;
import org.mapstruct.factory.Mappers;

import java.util.List;

@Mapper
public abstract class ClubMapper {

    public static final ClubMapper INSTANCE = Mappers.getMapper(ClubMapper.class);

    public abstract ClubEntity requestToEntity(ClubRequest clubRequest);
    public abstract List<ClubDto> EntityToDtoList(List<ClubEntity> clubEntityList);
    public abstract ClubDto entityToDto(ClubEntity clubEntity);
}
*/
