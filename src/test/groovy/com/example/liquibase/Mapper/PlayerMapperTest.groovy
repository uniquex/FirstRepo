/*
package com.example.liquibase.Mapper

import com.example.liquibase.Entity.PlayerEntity
import com.example.liquibase.Model.Mapper.PlayerMapper
import spock.lang.Specification

class PlayerMapperTest extends Specification {

    def "entityToDto"(){
        given:
        def entity = PlayerEntity.builder().name("Tural").build()

        when:
        def result = PlayerMapper.INSTANCE.entityToDto(entity)

        then:
        entity.name == result.name
    }

}
*/
